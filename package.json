{
  "name": "breadcrumb-recall",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agentic work-state memory proof for Breadcrumb Recall.",
  "scripts": {
    "api:dev": "tsx src/api/server.ts",
    "api:smoke": "tsx src/scripts/api-smoke.ts",
    "build": "tsc -p tsconfig.build.json",
    "smoke": "tsx src/smoke.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@huggingface/transformers": "3.8.1",
    "pg": "^8.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
